# state-trade-map

Interactive US state trade flow map using `year/2019/US/domestic/state_trade_flows.csv`.

Open `index.html` from a local web server so the browser can fetch the CSV files. The page centers on the dataset's `level` field as "amount spent" and also includes an `employment_impact` toggle.
